# Extract the tags from the given raw input
# You can see an example of the current extractor's input data on the right side
#
# The output is either
# - A flat array containing the extracted tags
#   `return ["some", "tags"]`
# - An array of "tags with categories"
#   `return [
#     {"category": "some_category", "tag": "some_tag"},
#     {"category": "other_category", "tag": "some_other_tag"},
#    ]`
#
# The code here is not sandboxed, so don't paste random stuff
# here.
#
def extract(input: dict) -> any:
    pass
